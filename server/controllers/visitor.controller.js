/**
 * Visitor Controller
 *
 * Placeholder — will be implemented in Phase 6.
 *
 * Planned endpoints:
 *   POST  /api/visitors            – Pre-register visitor
 *   PATCH /api/visitors/:id/entry  – Log entry time
 *   PATCH /api/visitors/:id/exit   – Log exit time
 *   GET   /api/visitors            – Admin: full visitor log
 *   GET   /api/visitors/my         – Student: own visitors
 */
