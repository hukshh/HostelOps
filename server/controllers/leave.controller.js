/**
 * Leave Controller
 *
 * Placeholder — will be implemented in Phase 5.
 *
 * Planned endpoints:
 *   POST  /api/leaves              – Student applies for leave
 *   GET   /api/leaves/pending      – Warden fetches pending leaves
 *   PATCH /api/leaves/:id/review   – Approve/reject + remark
 *   GET   /api/leaves/:id/pass     – Download leave pass PDF
 */
